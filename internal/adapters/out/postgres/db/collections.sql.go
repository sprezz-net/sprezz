// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: collections.sql

package db

import (
	"context"
)

const getInboxPayloads = `-- name: GetInboxPayloads :many
SELECT q.payload
FROM actor_inbox_deliveries d
JOIN inbound_activity_queue q ON q.activity_iri = d.activity_iri
WHERE d.actor_iri = $1
ORDER BY d.created_at DESC
LIMIT $2 OFFSET $3
`

type GetInboxPayloadsParams struct {
	ActorIri string `json:"actor_iri"`
	Limit    int32  `json:"limit"`
	Offset   int32  `json:"offset"`
}

func (q *Queries) GetInboxPayloads(ctx context.Context, arg GetInboxPayloadsParams) ([][]byte, error) {
	rows, err := q.db.Query(ctx, getInboxPayloads, arg.ActorIri, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := [][]byte{}
	for rows.Next() {
		var payload []byte
		if err := rows.Scan(&payload); err != nil {
			return nil, err
		}
		items = append(items, payload)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getOutboxPayloads = `-- name: GetOutboxPayloads :many
SELECT payload
FROM outbound_activity_queue
WHERE actor_iri = $1
ORDER BY created_at DESC
LIMIT $2 OFFSET $3
`

type GetOutboxPayloadsParams struct {
	ActorIri string `json:"actor_iri"`
	Limit    int32  `json:"limit"`
	Offset   int32  `json:"offset"`
}

func (q *Queries) GetOutboxPayloads(ctx context.Context, arg GetOutboxPayloadsParams) ([][]byte, error) {
	rows, err := q.db.Query(ctx, getOutboxPayloads, arg.ActorIri, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := [][]byte{}
	for rows.Next() {
		var payload []byte
		if err := rows.Scan(&payload); err != nil {
			return nil, err
		}
		items = append(items, payload)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
