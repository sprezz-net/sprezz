// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: actor.sql

package db

import (
	"context"
)

const getActorQuadsByIRI = `-- name: GetActorQuadsByIRI :many
SELECT
    q.graph_id,
    s_term.value AS subject,
    p_term.value AS predicate,
    o_term.value AS object
FROM rdf_quads q
JOIN rdf_graphs g ON q.graph_id = g.id
JOIN rdf_dictionary s_term ON q.subject_id = s_term.id
JOIN rdf_dictionary p_term ON q.predicate_id = p_term.id
JOIN rdf_dictionary o_term ON q.object_id = o_term.id
WHERE q.graph_id = (
      -- Instantly isolate the latest historical graph version block explicitly matching the actor's stable ID
      SELECT inner_q.graph_id
      FROM rdf_quads inner_q
      JOIN rdf_graphs inner_g ON inner_q.graph_id = inner_g.id
      JOIN rdf_dictionary inner_s ON inner_q.subject_id = inner_s.id
      WHERE inner_s.value = $1
      ORDER BY inner_q.graph_id DESC
      LIMIT 1
  )
`

type GetActorQuadsByIRIRow struct {
	GraphID   int64  `json:"graph_id"`
	Subject   string `json:"subject"`
	Predicate string `json:"predicate"`
	Object    string `json:"object"`
}

func (q *Queries) GetActorQuadsByIRI(ctx context.Context, value string) ([]GetActorQuadsByIRIRow, error) {
	rows, err := q.db.Query(ctx, getActorQuadsByIRI, value)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GetActorQuadsByIRIRow{}
	for rows.Next() {
		var i GetActorQuadsByIRIRow
		if err := rows.Scan(
			&i.GraphID,
			&i.Subject,
			&i.Predicate,
			&i.Object,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getActorQuadsByUsername = `-- name: GetActorQuadsByUsername :many
SELECT
    q.graph_id,
    s_term.value AS subject,
    p_term.value AS predicate,
    o_term.value AS object
FROM rdf_quads q
JOIN rdf_graphs g ON q.graph_id = g.id
JOIN rdf_dictionary s_term ON q.subject_id = s_term.id
JOIN rdf_dictionary p_term ON q.predicate_id = p_term.id
JOIN rdf_dictionary o_term ON q.object_id = o_term.id
WHERE q.graph_id = (
      -- Isolate the latest known graph version associated with this actor handle
      SELECT inner_q.graph_id
      FROM rdf_quads inner_q
      JOIN rdf_graphs inner_g ON inner_q.graph_id = inner_g.id
      JOIN rdf_dictionary inner_s ON inner_q.subject_id = inner_s.id
      JOIN rdf_dictionary inner_p ON inner_q.predicate_id = inner_p.id
      JOIN rdf_dictionary inner_o ON inner_q.object_id = inner_o.id
      WHERE inner_p.value = 'https://www.w3.org/ns/activitystreams#preferredUsername'
        -- Explicit parameter type overrides for clean sqlc struct generation:
        AND inner_o.value = $1::text
        AND inner_s.value LIKE $2::text
      ORDER BY inner_q.graph_id DESC
      LIMIT 1
  )
`

type GetActorQuadsByUsernameParams struct {
	Username     string `json:"username"`
	TenantPrefix string `json:"tenant_prefix"`
}

type GetActorQuadsByUsernameRow struct {
	GraphID   int64  `json:"graph_id"`
	Subject   string `json:"subject"`
	Predicate string `json:"predicate"`
	Object    string `json:"object"`
}

func (q *Queries) GetActorQuadsByUsername(ctx context.Context, arg GetActorQuadsByUsernameParams) ([]GetActorQuadsByUsernameRow, error) {
	rows, err := q.db.Query(ctx, getActorQuadsByUsername, arg.Username, arg.TenantPrefix)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GetActorQuadsByUsernameRow{}
	for rows.Next() {
		var i GetActorQuadsByUsernameRow
		if err := rows.Scan(
			&i.GraphID,
			&i.Subject,
			&i.Predicate,
			&i.Object,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getTenantDomainByID = `-- name: GetTenantDomainByID :one
SELECT domain_name
FROM server_tenants
WHERE id = $1
`

func (q *Queries) GetTenantDomainByID(ctx context.Context, id int32) (string, error) {
	row := q.db.QueryRow(ctx, getTenantDomainByID, id)
	var domain_name string
	err := row.Scan(&domain_name)
	return domain_name, err
}
